"""The application layer — ports, services and use cases.

Ports define the boundaries (driving = inbound, driven = outbound). Services
orchestrate and never catch. Use cases are the thin, mockable entry points.
"""

from __future__ import annotations
